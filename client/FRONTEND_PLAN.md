# Moved

This document now lives at **`doc/FRONTEND_PLAN.md`** (per the repo convention:
non-README markdown belongs in `doc/`). See there for the frontend plan, status,
and the open platform-console proposal.
