{
  "_note": "Committed allow-list for scripts/check-response-contract.js. Add a field with a one-line reason in _reasons; remove it when the underlying source of truth is fixed.",
  "_reasons": {
    "actual_count": "financial_dictionary.rules spendSeries builds `row[`${lens}_count`]` from SPEND_LENSES at runtime — no literal key exists to find. Sibling of the already-listed estimated_count.",
    "bank_account_number": "Form state, not a response field: employee_allowance/employee.bank_block is a jsonb blob, and EmployeeDraft flattens it into one input per key so the edit form has something to bind to. draftToPayload folds them back into bank_block on the way out.",
    "bank_branch": "Form state, not a response field: employee_allowance/employee.bank_block is a jsonb blob, and EmployeeDraft flattens it into one input per key so the edit form has something to bind to. draftToPayload folds them back into bank_block on the way out.",
    "bank_iban": "Form state, not a response field: employee_allowance/employee.bank_block is a jsonb blob, and EmployeeDraft flattens it into one input per key so the edit form has something to bind to. draftToPayload folds them back into bank_block on the way out.",
    "bank_swift": "Form state, not a response field: employee_allowance/employee.bank_block is a jsonb blob, and EmployeeDraft flattens it into one input per key so the edit form has something to bind to. draftToPayload folds them back into bank_block on the way out.",
    "client_scoped": "DOCUMENT_TYPE config stored inside a dictionary_item jsonb blob, so it is not a column and never will be. Sibling of the already-listed client_visible.",
    "committed_count": "Same computed key as actual_count. Emitted, but only ever as a template literal.",
    "exclude_id": "Input, not output: read as req.body.exclude_id by master/_shared/actions.js. DedupeInput is a request shape.",
    "field_type": "Custom-field definitions live inside a `setting` jsonb blob (security/setting/setting.rules.js), so no column backs them.",
    "identity_from": "sendpoint.repo.listWithBindings emits this inside a json_build_object() aggregate so the admin screen gets a send point and its bindings in one query rather than N+1 over 22 rows. The scanner only recognises `SELECT ... AS alias`, so a key built by json_build_object is invisible to it. The field IS sent.",
    "identity_name": "sendpoint.repo.listWithBindings emits this inside a json_build_object() aggregate so the admin screen gets a send point and its bindings in one query rather than N+1 over 22 rows. The scanner only recognises `SELECT ... AS alias`, so a key built by json_build_object is invisible to it. The field IS sent."
  },
  "tables": [
    "ageing",
    "app_role",
    "dossier_visible",
    "false",
    "finals",
    "first_out",
    "grant",
    "hits",
    "jsonb_to_recordset",
    "patch",
    "span",
    "unlinked_credit"
  ],
  "allow": [
    "actor_email_snapshot",
    "actual_count",
    "advance_open",
    "bank_account_number",
    "bank_branch",
    "bank_iban",
    "bank_swift",
    "client_scoped",
    "client_visible",
    "coa_aux_account",
    "committed_count",
    "container_type_label",
    "credit_note_id",
    "cv_read",
    "date_field",
    "deactivation_reason",
    "declaration_id",
    "default_tax_jurisdiction_id",
    "discrepancy_summary",
    "dissolution_date",
    "duplicate_candidates",
    "earning_lines",
    "entity_label",
    "estimated_count",
    "exclude_id",
    "failed_jurisdictions",
    "field_type",
    "gl_parity",
    "hard_block_reason",
    "hard_blocked_at",
    "header_note_fr",
    "holiday_date",
    "identity_from",
    "identity_name",
    "in_force",
    "in_use",
    "include_completed",
    "incorporation_country",
    "incorporation_date",
    "incorporation_place",
    "is_2fa_enabled",
    "is_accounting_connected",
    "is_ad_hoc",
    "is_late",
    "is_sensitive",
    "item_fields",
    "last_message",
    "last_sync",
    "ledger_scope",
    "legal_mentions_fr",
    "line_count",
    "linked_client_id",
    "linked_supplier_id",
    "marks_token",
    "minutes_late",
    "next_cursor",
    "numbering_reset",
    "oauth_hint",
    "payroll_country",
    "pending_changes",
    "primary_address",
    "primary_contact",
    "rate_history",
    "reward_amount",
    "reward_status",
    "risk_tier",
    "share_capital_paid_up",
    "share_class",
    "signed_vault_id",
    "staff_no",
    "status_changed_at",
    "step_count",
    "system_code",
    "table_name",
    "tax_office_ref",
    "tax_residency_country",
    "total_employer_charges",
    "usage_total",
    "variance_committed_actual",
    "variance_estimated_actual",
    "vat_registered",
    "weighted_score",
    "withholding_rate"
  ]
}
