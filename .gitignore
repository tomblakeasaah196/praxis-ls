# Dependencies
node_modules/
.pnpm-store/
.yarn/cache
.yarn/install-state.gz

# Build output
dist/
build/
*.tsbuildinfo
# Ladle component workbench (client/.ladle, `npm run workbench`)
workbench-dist/
.ladle/build/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pino-*.log

# Test coverage
coverage/
.nyc_output/

# OS files
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
*.iml

# Uploaded / generated media (per-environment)
uploads/temp/*
uploads/staging/*
!uploads/temp/.gitkeep
!uploads/staging/.gitkeep
media/originals/*
media/processed/*
media/posters/*
!media/originals/.gitkeep
!media/processed/.gitkeep
!media/posters/.gitkeep
media/
data/
# …but packages/shared/data is SOURCE (the canonical country reference the API,
# the seed and the client picker all import), not runtime data — keep it tracked.
!packages/shared/data/
!packages/shared/data/**

# Database dumps
*.dump
*.sql.gz
backups/

# Deploy state written on the server by scripts/deploy.sh — records the running
# and previous build so scripts/rollback.sh has something to point at. Host
# state, not source (audit TEST-D1).
.deploy-state/

# AI / Vector stores (if any local cache)
.embeddings-cache/

# Misc
.cache/
tmp/

# Claude Code
.claude/
client folder for hub-system/hub-system.zip
dump.rdb

# Compiled Vite config. `vite.config.ts` is the source of truth, but Vite resolves
# vite.config.js BEFORE vite.config.ts — a committed build artifact silently shadows
# the real config and every later edit to the .ts becomes a no-op.
# Transient entry written by client/scripts/check-shared.mjs; removed on exit,
# listed here so a crashed run cannot leave it committable.
client/.shared-probe.generated.js

client/vite.config.js
client/vite.config.d.ts
platform-console/vite.config.js
platform-console/vite.config.d.ts
test-failures.txt
client/test-errors.txt
